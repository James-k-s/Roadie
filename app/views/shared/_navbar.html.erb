<nav class="navbar navbar-expand-lg navbar-custom">
  <div class="container-fluid">
  <%= link_to image_tag("RoadieTemporaryLogo.png", width: "55px"), root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav">

        <li class="nav-item"><%= link_to "About", about_path, class: "nav-links" %></li>

        <li class="nav-item"><%= link_to "Bands", bands_path, class: "nav-links" %></li>
        <li class="nav-item"><%= link_to "Musicians", users_path, class: "nav-links" %></li>
        <li class="nav-item"><%= link_to "Posts", posts_path, class: "nav-links" %></li>

        <% if user_signed_in? && BandMember.find_by(user_id: current_user.id) %>
          <li class="nav-item"><%= link_to "My band", band_path(@user_band), class: "nav-links" %> </li>
        <% elsif user_signed_in? %>
          <li class="nav-item"><%= link_to "Create Band", new_user_band_path(current_user), class: "nav-links" %></li>
        <% end %>

        <% if user_signed_in? %>
          <li class="nav-item"><%= link_to "My profile", user_path(current_user), class: "nav-links" %></li>
        <% end %>
      </ul>
      <% if user_signed_in? %>
        <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete }, class: "nav-links log" %>
      <% else %>
        <%= link_to "Login", new_user_session_path, class: "nav-links log" %>
        <%= link_to "Sign Up", new_user_registration_path, class: "nav-links log" %>
      <% end %>
    </div>
  </div>
</nav>
